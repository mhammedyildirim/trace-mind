# TraceMind Environment Configuration
# 
# IMPORTANT SECURITY NOTICE:
# - Do NOT commit real API keys to version control
# - GEMINI_API_KEY must be provided at runtime in production
# - This file is a template only - copy to .env and fill in your values
# - .env files are excluded from Docker images via .dockerignore

# Server Configuration
PORT=3000
LOG_LEVEL=info

# Google Gemini Configuration
# Get your API key from: https://makersuite.google.com/app/apikey
# REQUIRED: Must be provided at runtime - never hardcode in Docker images
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-1.5-flash

# Analysis Configuration (optional)
MAX_ANALYSIS_TIMEOUT_MS=10000
MIN_TRACE_DURATION_MS=50  # Skip analysis for very fast traces
